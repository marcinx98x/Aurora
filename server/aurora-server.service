[Unit]
Description=Aurora Music Server
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/aurora-server
EnvironmentFile=-/opt/aurora-server/.env
ExecStart=/opt/aurora-server/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000 --workers 2
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
